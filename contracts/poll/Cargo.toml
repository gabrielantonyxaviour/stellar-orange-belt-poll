[package]
name = "poll"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
reward-token = { path = "../reward-token" }
soroban-sdk = { workspace = true, features = ["testutils"] }
